# 大模型八股（Transformer 主线版）

## 0. 从自然语言到可计算张量：Tokenization → Embedding → Positional Encoding（覆盖题 8、3）

自然语言输入后，第一步是 **Tokenization**，其目标是把字符串映射成离散 token id 序列，核心矛盾是词表大小与序列长度的折中：词表太小会导致句子变长，词表太大又会造成稀疏和训练成本上升；经典做法是 BPE、WordPiece、Unigram（SentencePiece）。第二步是 **Embedding**，把离散 id 映射为连续向量，若词表大小为 $V$、隐藏维度为 $d_{model}$，则 embedding 矩阵 $E\in\mathbb{R}^{V\times d_{model}}$，输入 token id $t_i$ 的向量为 $x_i=E[t_i]$。第三步是 **Positional Encoding**，因为自注意力本身对置换不敏感（不含顺序偏置），必须显式注入位置信息；工程实现里常把位置编码与 token embedding 直接相加（绝对位置）或在 attention 分数层面注入（相对/旋转位置）。常用包：`transformers`（`AutoTokenizer`、`AutoModel`）、`tokenizers`（高性能分词器训练与推理）、`sentencepiece`（BPE/Unigram）、`torch.nn.Embedding`（词嵌入）。

### 为什么必须要位置编码？

设输入序列向量矩阵为 $X=[x_1,\dots,x_n]^T$，自注意力输出是
$$
\mathrm{Attn}(X)=\mathrm{Softmax}\left(\frac{XW_Q(XW_K)^T}{\sqrt{d_k}}\right)XW_V
$$
若对输入做同一个置换矩阵 $P$（即打乱 token 顺序），则 $X' = PX$，可推得
$$
\mathrm{Attn}(PX)=P\,\mathrm{Attn}(X)
$$
这说明 attention 对顺序本身没有“绝对语义”，只会随输入一起重排，因此模型无法区分“我爱你”和“你爱我”的位置差异，必须引入位置编码破坏这种置换等变性。

### 为什么早期用 Sin/Cos 位置编码？

经典 Transformer 位置编码定义为
$$
PE(pos,2i)=\sin\left(pos/10000^{2i/d_{model}}\right),\quad
PE(pos,2i+1)=\cos\left(pos/10000^{2i/d_{model}}\right)
$$
其优点有三点：第一，不增加可学习参数；第二，不同频率覆盖长短尺度；第三，最关键是可线性表达相对位移，因为
$$
\sin(a+b)=\sin a\cos b + \cos a\sin b,\quad
\cos(a+b)=\cos a\cos b - \sin a\sin b
$$
所以位置 $pos+k$ 的编码可由位置 $pos$ 的编码经线性变换得到，这对泛化到更长序列有帮助。

### 还有哪些经典实现？重点：RoPE（旋转位置编码）

RoPE（Rotary Positional Embedding）把位置编码从“向量相加”改成“对 $Q,K$ 做旋转”，在二维子空间内可直接写成标量形式：
$$
q^{(m)}_{2i}=q_{2i}\cos\theta_{m,i}-q_{2i+1}\sin\theta_{m,i}
$$
$$
q^{(m)}_{2i+1}=q_{2i}\sin\theta_{m,i}+q_{2i+1}\cos\theta_{m,i}
$$
其中 $m$ 是位置索引。对 $k$ 同样旋转后，内积满足只与相对位移有关：
$$
(R_m q)^\top(R_n k)=q^\top R_{n-m}k
$$
因此天然编码相对位置信息。工程里还常见 ALiBi（线性偏置）、T5 相对位置偏置等。PyTorch 生态可用：`transformers` 中模型实现、`xformers` 与社区 RoPE 模块，或自行用 `torch.view_as_complex` / `torch.einsum` 实现旋转。

---

## 1. Encoder / Decoder 是什么？attention 区别是什么？（覆盖题 1）

**Encoder** 是“读入并编码上下文”的堆叠模块，典型用于 BERT、T5 Encoder、ViT 等；**Decoder** 是“自回归生成下一 token”的堆叠模块，典型用于 GPT/LLaMA/Qwen。二者最核心的 attention 差异是：Encoder 使用 **双向 self-attention**（可看见全句），Decoder 使用 **因果 self-attention**（只能看当前位置之前的 token）。在 Encoder-Decoder 架构（如原始 Transformer、T5）中，Decoder 还多了一个 **cross-attention**，即 Query 来自 Decoder 隐状态，Key/Value 来自 Encoder 输出，用于“边生成边读取源句信息”。现代大语言模型多数是 Decoder-only，因为训练目标和推理链路更统一，扩展性更好；但机器翻译等强对齐任务中 Encoder-Decoder 仍有优势。

---

## 2. 单头 Attention 计算全链路（重点）（覆盖题 4）

给定输入隐藏状态 $X\in\mathbb{R}^{n\times d_{model}}$，单头 attention 先做线性投影：
$$
Q=XW_Q,\quad K=XW_K,\quad V=XW_V,
$$
其中 $W_Q\in\mathbb{R}^{d_{model}\times d_k}$，$W_K\in\mathbb{R}^{d_{model}\times d_k}$，$W_V\in\mathbb{R}^{d_{model}\times d_v}$。未归一化打分矩阵
$$
S=QK^T\in\mathbb{R}^{n\times n},\quad S_{ij}=q_i\cdot k_j.
$$
随后做缩放与掩码：
$$
S^{mask} = \frac{S}{\sqrt{d_k}} + M
$$
其中 $M$ 为掩码矩阵：允许位置取 $0$，禁止位置取 $-\infty$（实现上常用很小负数）。然后做按行 softmax 得到注意力权重
$$
A_{ij}=\frac{\exp(S^{mask}_{ij})}{\sum_{j'}\exp(S^{mask}_{ij'})},\quad A\mathbf{1}=\mathbf{1}
$$
最后输出
$$
O=AV.
$$

### 为什么要除以 $\sqrt{d_k}$？

若 $q_i,k_j$ 分量独立同分布、均值 0 方差 1，则点积
$$
q_i\cdot k_j = \sum_{t=1}^{d_k} q_{it}k_{jt}
$$
其方差近似为 $d_k$，即 $\mathrm{Var}(q\cdot k)\propto d_k$。当 $d_k$ 大时，softmax 输入幅值过大，分布会过于尖锐，梯度趋近 0，训练不稳定。缩放后
$$
\mathrm{Var}\left(\frac{q\cdot k}{\sqrt{d_k}}\right)\approx 1
$$
可使 softmax 工作在更健康区间，避免过早饱和。

### 掩码机制是什么？

1）**Padding Mask**：屏蔽补齐 token，避免无效位参与注意力；2）**Causal Mask**：在 Decoder 自回归中，位置 $i$ 不能看未来 $j>i$，即上三角置 $-\infty$。写成
$$
M_{ij}=\begin{cases}
0,& j\le i\\
-\infty,& j>i
\end{cases}
$$
这样 softmax 后未来位权重为 0，保证训练-推理一致性。

### 归一化操作是什么？

在 attention 内部，归一化是对每个 query 行做 softmax，使该 query 对所有 key 的权重和为 1（概率分布）；在 block 层面还有 LayerNorm（后文详讲），二者作用不同：softmax 是“概率归一化”，LayerNorm 是“特征标准化”。

PyTorch 基础实现组件：`torch.matmul`、`torch.softmax`、`torch.triu`（构造因果掩码）、`torch.nn.functional.scaled_dot_product_attention`（PyTorch 2.x 统一接口，后端可自动使用高效内核）。

---

## 3. 从单头到多头：MHA / MQA / GQA（覆盖题 2）

### MHA（Multi-Head Attention）

MHA 把隐藏维度拆成 $h$ 个头，每头有独立 $W_Q^i,W_K^i,W_V^i$，得到
$$
\mathrm{head}_i=\mathrm{Attn}(XW_Q^i, XW_K^i, XW_V^i),\quad
\mathrm{MHA}(X)=\mathrm{Concat}(\mathrm{head}_1,\dots,\mathrm{head}_h)W_O.
$$
优势是每个头可学习不同关系模式（语法、指代、长程依赖）；缺点是推理期 KV cache 占用大，因为每个头都要存 K/V。

### MQA（Multi-Query Attention）

MQA 的核心是“**多 Query，单共享 K/V**”：不同头的 $Q$ 保留，但所有头共享一套 $K,V$。这样 KV cache 从与头数 $h$ 成正比，降到近似常数级（相对 MHA 大幅降低），极大优化长上下文推理吞吐；代价是表达能力可能略降，因为 K/V 头间多样性减少。

### GQA（Grouped-Query Attention）

GQA 是 MHA 与 MQA 的折中：把 $h$ 个 query 头分成 $g$ 组，每组共享 K/V，通常 $1<g<h$。这样在质量和吞吐之间得到更平衡：比 MHA 更省显存和带宽，比 MQA 保留更多头间差异。LLaMA 系列后续版本广泛使用 GQA。若设每组包含 $h/g$ 个 query 头，则 KV cache 相对 MHA 大约可降到 $g/h$。

工程结论：训练看质量上限，推理看 KV 带宽瓶颈。长上下文线上服务中，GQA/MQA 往往比纯 MHA 更具性价比。

---

## 4. LayerNorm、残差连接、Softmax 在 Block 里的角色（覆盖题 9、10）

### LayerNorm 是什么？规则是什么？

LayerNorm 对“单个 token 的特征维度”做标准化。对向量 $x\in\mathbb{R}^{d}$：
$$
\mu=\frac{1}{d}\sum_{i=1}^{d}x_i,\quad
\sigma^2=\frac{1}{d}\sum_{i=1}^{d}(x_i-\mu)^2,
$$
$$
\mathrm{LN}(x)_i = \gamma_i\frac{x_i-\mu}{\sqrt{\sigma^2+\epsilon}}+\beta_i.
$$
其中 $\gamma,\beta$ 可学习。它的作用是稳定特征分布、改善梯度传播、降低学习率敏感性。大模型常见 Pre-LN 结构（先 LN 再子层）以提升深层训练稳定性。

PyTorch 对应：`torch.nn.LayerNorm`。

### 残差连接（Residual Connection）

残差是
$$
y = x + F(x)
$$
其核心是给梯度提供“近似恒等”的捷径，缓解深层网络退化。Transformer 中每个 attention 子层和 FFN 子层都配残差，形成“LN + 子层 + 残差”模式。没有残差时，深层模型很难训练到稳定收敛。

### Softmax 讲解

Softmax 将打分向量 $z$ 映射为概率：
$$
\mathrm{softmax}(z_i)=\frac{e^{z_i}}{\sum_j e^{z_j}}.
$$
在 attention 中它把 query 对各 key 的相关性转成权重分布；在词表输出层，它把 logits 转成下一个 token 的分布。数值稳定实现通常先减最大值：
$$
\mathrm{softmax}(z_i)=\frac{e^{z_i-z_{max}}}{\sum_j e^{z_j-z_{max}}}
$$
避免指数溢出。PyTorch：`torch.nn.functional.softmax`。

---

## 5. FlashAttention 是什么？为什么要这么做？（覆盖题 5）

FlashAttention 本质是“**精确 attention 的 IO-aware 重排算法**”，不是近似 attention。传统实现会显式物化 $n\times n$ 注意力矩阵，长序列时显存和 HBM 读写成为瓶颈；FlashAttention 通过分块（tiling）在 SRAM/shared memory 内流式计算 $QK^T$、在线 softmax、再乘 $V$，避免把完整注意力矩阵写回 HBM，从而显著减少内存访问复杂度并提升吞吐。其关键数学点是在线 softmax 可分块累积：维护每行的运行最大值与归一化因子，保证与标准 softmax 数值等价。工程收益：更低显存占用、更高训练/推理速度、支持更长上下文。常见实现入口：PyTorch 2.x 的 `scaled_dot_product_attention`（会根据后端选择 flash/mem-efficient/math kernel）、`flash-attn` 库、`xformers`。

---

## 6. KV Cache 是什么？为什么要这么做？（覆盖题 6）

自回归解码时，第 $t$ 步预测只新增一个 token；若每步都重算全部历史 token 的 K/V，复杂度会浪费巨大。KV Cache 的思想是把历史步的 $K_{1:t-1},V_{1:t-1}$ 缓存起来，第 $t$ 步只计算新 token 的 $k_t,v_t$ 并拼接到缓存，attention 直接对缓存做检索。这样每步从“重复全历史投影”变为“增量投影 + 读缓存”，显著降低时延。若不考虑并行 batch，整体解码从朴素重算的高冗余流程降到更接近线性增量流程。代价是显存占用增加，且上下文越长缓存越大，所以 MQA/GQA 与量化 KV cache（如 fp8/int8）在工程中经常组合使用。Hugging Face 中常见开关是 `use_cache=True`，服务框架会维护每层每头的 past_key_values。

---

## 7. 如果手写 `model.py/train.py`，超参数层怎么设计？参数有哪些、推荐范围？（覆盖题 7）

建议把超参数分为 6 组：**模型结构、注意力细节、优化器与学习率、训练稳定性、数据与序列、系统与并行**。

### 7.1 模型结构超参数（`model.py`）

- `vocab_size`：词表大小，常见 32k~200k。
- `d_model`：隐藏维度，小模型 512~2048，中大模型 3072~8192+。
- `n_layers`：层数，12~80+。
- `n_heads`：注意力头数，8~64。
- `head_dim`：常见 64/80/96/128（一般满足 `d_model = n_heads * head_dim`）。
- `ffn_hidden_dim`：通常为 `d_model` 的 2.5~4 倍（SwiGLU 常见约 2.67~3.5 倍有效宽度）。
- `max_seq_len`：2k、4k、8k、32k、128k（与 RoPE scaling/训练策略相关）。
- `norm_type`：`LayerNorm` 或 `RMSNorm`（大模型常用 RMSNorm）。
- `attn_type`：`MHA`/`GQA`/`MQA`。
- `dropout`：预训练大模型常接近 0~0.1，微调常 0~0.2。

### 7.2 训练超参数（`train.py`）

- 优化器：`AdamW`（`betas=(0.9,0.95/0.999)`, `eps=1e-8`）。
- 学习率：预训练常见峰值 `1e-4 ~ 5e-4`（随 batch/token budget 调整）；全参微调常 `5e-6 ~ 5e-5`；LoRA 常 `1e-4 ~ 5e-4`。
- `weight_decay`：0.01~0.1（预训练常 0.1 左右）。
- `warmup_ratio`：1%~5%。
- 调度器：cosine / linear decay。
- `global_batch_size`：按 token 预算定义更合理（如每步总 token 数固定）。
- `grad_accum_steps`：显存不足时提高以维持全局 batch。
- `max_grad_norm`：0.5~1.0（梯度裁剪）。
- 混精度：`bf16` 优先，其次 `fp16`；必要时开启 loss scale。

### 7.3 稳定性与效率参数

- `gradient_checkpointing=True`（省显存）。
- `flash_attention=True`（若硬件支持）。
- `use_kv_cache`（推理打开，训练通常关闭）。
- `label_smoothing`：0~0.1（任务相关）。
- `z_loss`（可选，稳定 logits）。

### 7.4 推荐配置思路

先固定 token budget 与吞吐目标，再倒推 `d_model/n_layers`；先保证可训练稳定，再做规模扩展。工程上应把配置做成分层 schema（基础默认 + 模型族覆盖 + 实验覆盖），并记录完整实验元数据（seed、git sha、数据版本、config hash）。

PyTorch 相关包：`torch`, `torch.nn`, `torch.optim`, `torch.distributed`, `torch.cuda.amp`; 训练框架可选 `lightning`, `accelerate`, `deepspeed`, `fairscale`。

---

## 8. 一个完整 Transformer Block 的顺序总结（把前面串起来）

以 Pre-LN Decoder block 为例，对输入 $x$：
$$
h_1 = x + \mathrm{Attn}(\mathrm{LN}(x))
$$
$$
h_2 = h_1 + \mathrm{FFN}(\mathrm{LN}(h_1))
$$
其中 Attn 内部是“QKV 投影 → 缩放点积 + 掩码 → softmax → 加权求和”，FFN 常为两层 MLP（如 SwiGLU 变体）。整个模型在 token 维度循环自回归生成，推理端配合 KV cache，训练端配合 FlashAttention 与混精度来提升效率。

---

## 9. 面试高频追问建议（答题策略）

当面试官追问“为什么”时，建议按 **数学动机 → 工程瓶颈 → 具体实现** 三步答：例如“为什么除 $\sqrt{d_k}$”先答方差随维度增大导致 softmax 饱和，再答梯度不稳定，再补实现就是 scaled dot-product；问 FlashAttention 就先答 IO 瓶颈，再答分块在线 softmax，再答 PyTorch 2.x 可直接走 `scaled_dot_product_attention`。这样比只背概念更像真正做过训练和部署的人。